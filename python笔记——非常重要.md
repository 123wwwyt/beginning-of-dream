# python常规知识

## 12.5

### 一。c和python的完全不同之处主要是格式
~~~
1.python  if 后面不用加括号
2.函数与代码复用  ,非常重要
3。python格式化输出：

f插值法，或者format方法
~~~
~~~
%d：整数。
%f：浮点数。
%e：科学计数法。
%s：字符串。
%x：十六进制整数。
%o：八进制整数。
~~~
~~~
4 a = eval(input())
print("{:-^20}".format(pow(a, 3)))

5  input函数获取输入是字符形式

6 python强调简洁，没有++或者--

7 虽然python和c语言的整体思路相同，但是python明显更加简洁
举个例子：对于三水仙花数的计算，python  可以对输入的数字进行字符化，然后由字符串切片来分别获得个十百位。
再分别计算，而对于c语言，还要设计一个除法程序来获得数字。
但是，为什么python 中三位数199%10   会得到由好多位小数的数字？

8 s[:-1]方式不输出最后一个逗号.
先生成字符的格式，最后再删除就行
~~~