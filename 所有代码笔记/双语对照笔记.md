# python和c语言双语对照

## 1.循环法，
这个没什么好说的。
python用 range()就可以了
## 2 在同一行输入两个变量：
c用scanf就行，但是  python要用.split方法。

eg    

比如我要获取输出正方形要输入一个数字和一个字符。
     a= input().split()   
     
      # .split方法默认是以空格为分割符，也就是你输入”8*“，但是中间没有空格，
这只相当与一个字符，所以可能会报错.并且，分割会直接生成列表哦

    b =int(a[0])   ///int 的强制转化，还是尽力不要用eval了 

## 3 逻辑运算符
Python：and  or  not  
C：&&||!

关键字Python：if不能用作变量名，而IF、If、iF可以

C：if、IF、If、iF均不能用作变量名


对于列表(list)这样的线性数据结构，还可以截取里面的一部分数据来使用，这就是切片。

~~~
SomeNumber= [10, 20, 40, 80, 160]
print(SomeNumber[1:3])
print(SomeNumber[1:])
print(SomeNumber[:3])
~~~
冒号，分隔开起始位置和终止位置

具体的数据起始位置和终止位置，但数据切片截取遵从“左闭右开”原则，


# 4  列表与遍历

python的列表功能更强大，并且遍历也方便，用for i in aaa   就可以了（aaa是一个列表）
